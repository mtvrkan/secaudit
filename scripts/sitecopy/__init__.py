"""Page copy for the generated site, one module per page.

Data only: every value here is a string a reader sees. The figures inside them are
substituted by `gen_site.py` from the repository, which is why none of them is a number.
"""
