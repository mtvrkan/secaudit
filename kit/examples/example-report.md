<!-- Generated by scripts/gen_example_report.py — do not edit by hand.
     This is a real Tier-0 scan of tests/fixtures/vulnerable-app, the deliberately vulnerable
     fixture in this repository, rendered by the same code path `secaudit --format md` uses.
     Regenerate with: python3 scripts/gen_example_report.py -->

# SecAudit report — `tests/fixtures/vulnerable-app`

**Backend:** none  ·  **Files scanned:** 23  ·  **Tools:** builtin-detectors, taint, structural, redos

## Summary

| Severity | Count |
|---|---|
| Critical | 10 |
| High | 40 |
| Medium | 23 |
| Low | 0 |
| Informational | 0 |

**Total findings:** 73

## Findings

### [Critical] Superglobal passed straight into a query call
- **Location:** `index.php:17`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-PHP-SQLI-SUPERGLOBAL` (builtin, confidence high, verdict unverified)
- **Evidence:** `return mysqli_query($conn, "SELECT * FROM users WHERE id = " . $_GET['id']);`
- **Fix:** Bind the value instead of pasting it: `$stmt = $pdo->prepare('… WHERE id = ?'); $stmt->execute([$id]);`. A superglobal inside the query TEXT is injection even when the call is named `prepare`.

### [Critical] Insecure deserialization (pickle.loads on untrusted data)
- **Location:** `py_app.py:36`
- **Class:** CWE-502 · OWASP A08  ·  **Detector:** `SEC-PY-PICKLE` (builtin, confidence high, verdict unverified)
- **Evidence:** `return pickle.loads(base64.b64decode(cookie))  # UNSAFE: never unpickle untrusted data`
- **Reachability:** `L35: load_session(cookie) (parameter) → L36: TAINT-PY-DESER argument 0`
- **Fix:** Use a safe format (JSON with a schema); never unpickle untrusted bytes.

### [Critical] SQL injection — untrusted input in the query string
- **Location:** `py_app.py:44`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `TAINT-PY-SQLI` (taint, confidence high, verdict unverified)
- **Evidence:** `return find_by_name(cursor, request.args["name"])   # UNSAFE: untrusted input, unbound`
- **Reachability:** `L44: request.args (request) → L44: passed to find_by_name(name) → L48: TAINT-PY-SQLI inside find_by_name()`
- **Fix:** Use bind parameters: cursor.execute(sql, (value,)).

### [Critical] SQL injection via string concatenation
- **Location:** `server.js:13`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-JS-SQLI` (builtin, confidence high, verdict unverified)
- **Evidence:** `const q = "SELECT * FROM users WHERE name = '" + req.query.name + "'";`
- **Reachability:** `L13: req.query.name (request) → L14: TAINT-JS-SQLI argument 0`
- **Fix:** Use parameterized queries / prepared statements; never concatenate input into SQL.

### [Critical] OS command injection (exec with concatenated input)
- **Location:** `server.js:19`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-JS-CMDI` (builtin, confidence high, verdict unverified)
- **Evidence:** `exec('ping -c 1 ' + req.query.host, (e, out) => res.send(out));`
- **Reachability:** `L19: req.query.host (request) → L19: TAINT-JS-CMDI argument 0`
- **Fix:** Use execFile/spawn with an argument array (no shell) and validate input.

### [Critical] OS command injection (exec with concatenated input)
- **Location:** `server.js:53`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-JS-CMDI` (builtin, confidence high, verdict unverified)
- **Evidence:** `exec('tar -czf /tmp/' + label + '.tgz /var/log/app', () => {});   // UNSAFE: shell string`
- **Reachability:** `L56: req.query.label (request) → L56: passed to archiveLogs(label) → L53: TAINT-JS-CMDI inside archiveLogs()`
- **Fix:** Use execFile/spawn with an argument array (no shell) and validate input.

### [Critical] Command injection — untrusted input reaches a shell
- **Location:** `server.js:66`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `TAINT-JS-CMDI` (taint, confidence high, verdict unverified)
- **Evidence:** `runReport(req.query.label);`
- **Reachability:** `L66: req.query.label (request) → L66: passed to runReport(label) → L36: TAINT-JS-CMDI inside util.js:runReport()`
- **Fix:** Use execFile/spawn with an argument array (no shell) and validate the input. The dangerous call is in `util.js:36`; fix it there, and check the other callers of that function.

### [Critical] SQL injection — untrusted input in the query string
- **Location:** `server.js:77`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `TAINT-JS-SQLI` (taint, confidence high, verdict unverified)
- **Evidence:** `` db.query(`SELECT * FROM products WHERE title LIKE '%${term}%'`, (e, r) => res.json(r)); ``
- **Reachability:** `L76: req.query.term (request) → L77: TAINT-JS-SQLI argument 0`
- **Fix:** Use a parameterized query: pass placeholders and bind the value as a parameter.

### [Critical] Insecure deserialization / code injection (eval)
- **Location:** `util.js:22`
- **Class:** CWE-95 · OWASP A03  ·  **Detector:** `SEC-JS-EVAL` (builtin, confidence high, verdict unverified)
- **Evidence:** `return eval('(' + str + ')');                        // UNSAFE: never eval untrusted data`
- **Reachability:** `L21: str (parameter) → L22: TAINT-JS-EVAL argument 0`
- **Fix:** Never eval untrusted input; parse data with JSON.parse.

### [Critical] OS command injection (exec with concatenated input)
- **Location:** `util.js:36`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-JS-CMDI` (builtin, confidence high, verdict unverified)
- **Evidence:** `` exec('generate-report --for ' + label);                // UNSAFE when `label` is untrusted ``
- **Fix:** Use execFile/spawn with an argument array (no shell) and validate input.

### [High] Secret baked into image ENV
- **Location:** `Dockerfile:4`
- **Class:** CWE-798 · OWASP A05  ·  **Detector:** `SEC-DOCKER-ENVSECRET` (builtin, confidence high, verdict unverified)
- **Evidence:** `[redacted] possible secret detected here (value not shown)`
- **Fix:** Inject secrets at runtime (--env / mounted file); never bake them into the image.

### [High] App Transport Security disabled
- **Location:** `Info.plist:9`
- **Class:** CWE-319 · OWASP A02  ·  **Detector:** `SEC-IOS-ATS` (builtin, confidence high, verdict unverified)
- **Evidence:** `<key>NSAllowsArbitraryLoads</key>`
- **Fix:** Keep ATS on; allow-list specific domains instead of disabling it globally.

### [High] WebView addJavascriptInterface (RCE surface)
- **Location:** `Mobile.kt:12`
- **Class:** CWE-749 · OWASP A05  ·  **Detector:** `SEC-ANDROID-JSIF` (builtin, confidence high, verdict unverified)
- **Evidence:** `web.addJavascriptInterface(Bridge(), "android")`
- **Fix:** Avoid it on untrusted content; require API 17+ and @JavascriptInterface, or drop it.

### [High] World-readable/writable file mode
- **Location:** `Mobile.kt:19`
- **Class:** CWE-732 · OWASP A04  ·  **Detector:** `SEC-ANDROID-WORLDPERM` (builtin, confidence high, verdict unverified)
- **Evidence:** `context.openFileOutput("session", Context.MODE_WORLD_READABLE).use {`
- **Fix:** Use MODE_PRIVATE; share via a FileProvider/content URI.

### [High] Insecure deserialization (BinaryFormatter)
- **Location:** `Repository.cs:14`
- **Class:** CWE-502 · OWASP A08  ·  **Detector:** `SEC-CS-DESER` (builtin, confidence high, verdict unverified)
- **Evidence:** `var formatter = new BinaryFormatter();`
- **Fix:** BinaryFormatter is unsafe and removed in modern .NET; use a safe serializer.

### [High] Insecure deserialization (ObjectInputStream)
- **Location:** `Service.java:12`
- **Class:** CWE-502 · OWASP A08  ·  **Detector:** `SEC-JAVA-DESER` (builtin, confidence high, verdict unverified)
- **Evidence:** `ObjectInputStream ois = new ObjectInputStream(in);`
- **Fix:** Avoid Java native deserialization of untrusted data; use a safe format.

### [High] OS command execution (Runtime.exec)
- **Location:** `Service.java:18`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-JAVA-EXEC` (builtin, confidence high, verdict unverified)
- **Evidence:** `return Runtime.getRuntime().exec("convert " + filename + " /tmp/out.png");`
- **Fix:** Use ProcessBuilder with an argument list; validate input.

### [High] LangChain dangerous execution explicitly enabled
- **Location:** `agent.py:10`
- **Class:** CWE-94 · OWASP A05  ·  **Detector:** `SEC-AI-LANGCHAIN-DANGER` (builtin, confidence high, verdict unverified)
- **Evidence:** `return SQLDatabaseChain.from_llm(llm, db, allow_dangerous_requests=True)`
- **Fix:** Do not enable dangerous execution on untrusted input; sandbox the agent and gate tools.

### [High] Dynamic code execution (eval/exec)
- **Location:** `agent.py:29`
- **Class:** CWE-95 · OWASP A03  ·  **Detector:** `SEC-PY-EVAL` (builtin, confidence high, verdict unverified)
- **Evidence:** `return eval(response.content)`
- **Reachability:** `L27: run_plan(llm) (parameter) → L29: TAINT-PY-EVAL argument 0`
- **Fix:** Never eval/exec untrusted input.

### [High] LLM/completion output flows into a code-exec sink
- **Location:** `agent.py:29`
- **Class:** CWE-94 · OWASP A05  ·  **Detector:** `SEC-AI-LLM-EXEC` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return eval(response.content)`
- **Fix:** Never execute model output; treat it as untrusted data and validate/parse it.

### [High] Disabled TLS verification (InsecureSkipVerify)
- **Location:** `api.go:16`
- **Class:** CWE-295 · OWASP A02  ·  **Detector:** `SEC-GO-TLS` (builtin, confidence high, verdict unverified)
- **Evidence:** `TLSClientConfig: &tls.Config{InsecureSkipVerify: true},`
- **Fix:** Keep certificate verification on; trust the proper CA bundle.

### [High] Broken JWT verification (alg:none accepted)
- **Location:** `auth.js:13`
- **Class:** CWE-347 · OWASP A07  ·  **Detector:** `SEC-JS-JWT-NONE` (builtin, confidence high, verdict unverified)
- **Evidence:** `if (header.alg === 'none') return payload;          // UNSAFE: unsigned token accepted`
- **Fix:** Pin the algorithm server-side, verify the signature, and check exp/aud.

### [High] Path traversal (user input in filesystem path)
- **Location:** `auth.js:26`
- **Class:** CWE-22 · OWASP A01  ·  **Detector:** `SEC-JS-PATHTRAV` (builtin, confidence high, verdict unverified)
- **Evidence:** `const file = path.join(__dirname, 'docs', req.query.file);  // ?file=../../etc/passwd`
- **Reachability:** `L26: req.query.file (request) → L27: TAINT-JS-PATH argument 0`
- **Fix:** Resolve then verify the result stays within an allowed base directory.

### [High] Mass assignment (whole request body copied to model)
- **Location:** `auth.js:33`
- **Class:** CWE-915 · OWASP A08  ·  **Detector:** `SEC-JS-MASSASSIGN` (builtin, confidence high, verdict unverified)
- **Evidence:** `Object.assign(user, req.body);                      // UNSAFE: no field allowlist`
- **Reachability:** `L33: req.body (request) → L33: TAINT-JS-MASSASSIGN argument 1`
- **Fix:** Copy only an explicit field allowlist; never bind the raw body.

### [High] Improper output handling → XSS (unsanitized markdown to innerHTML)
- **Location:** `chat.js:12`
- **Class:** CWE-79 · OWASP A03  ·  **Detector:** `SEC-JS-XSS` (builtin, confidence high, verdict unverified)
- **Evidence:** `bubble.innerHTML = marked.parse(msg);          // UNSAFE: raw HTML from model output`
- **Reachability:** `L8: msg (parameter) → L12: TAINT-JS-XSS-DOM argument 0`
- **Fix:** Sanitize rendered HTML with DOMPurify before assignment (or use textContent).

### [High] Improper output handling → XSS (unsanitized markdown to innerHTML)
- **Location:** `chat.js:18`
- **Class:** CWE-79 · OWASP A03  ·  **Detector:** `SEC-JS-XSS` (builtin, confidence high, verdict unverified)
- **Evidence:** `if (raw) el.innerHTML = marked.parse(raw);       // UNSAFE: same sink on history load`
- **Reachability:** `L16: el (parameter) → L18: TAINT-JS-XSS-DOM argument 0`
- **Fix:** Sanitize rendered HTML with DOMPurify before assignment (or use textContent).

### [High] GitHub Action pinned to a mutable branch ref
- **Location:** `ci.yml:10`
- **Class:** CWE-1357 · OWASP A03  ·  **Detector:** `SEC-CI-MUTABLE-ACTION` (builtin, confidence high, verdict unverified)
- **Evidence:** `- uses: tj-actions/changed-files@main`
- **Fix:** Pin third-party Actions to a full commit SHA — a branch is mutable and can be repointed at malicious code (the tj-actions/changed-files CVE-2025-30066 class, where the attacker moved the refs rather than publishing new code). Dependabot can still bump the pin. A version tag is better than a branch, a SHA is safest.

### [High] Remote script piped straight into a shell
- **Location:** `ci.yml:15`
- **Class:** CWE-494 · OWASP A03  ·  **Detector:** `SEC-SUPPLY-CURLPIPE` (builtin, confidence high, verdict unverified)
- **Evidence:** `run: curl -sSL https://example.com/install.sh | sh`
- **Fix:** Download, verify a checksum/signature, then run — never pipe curl/wget to a shell.

### [High] Hardcoded Stripe secret key
- **Location:** `config.json:4`
- **Class:** CWE-798 · OWASP A07  ·  **Detector:** `SEC-SECRET-STRIPE` (builtin, confidence high, verdict unverified)
- **Evidence:** `[redacted] possible secret detected here (value not shown)`
- **Fix:** Revoke and rotate immediately; load from a secret manager.

### [High] Hardcoded Google API key
- **Location:** `config.json:6`
- **Class:** CWE-798 · OWASP A07  ·  **Detector:** `SEC-SECRET-GOOGLE` (builtin, confidence high, verdict unverified)
- **Evidence:** `[redacted] possible secret detected here (value not shown)`
- **Fix:** Revoke and rotate the key; restrict it; load from a secret manager.

### [High] Privileged container
- **Location:** `deploy.yaml:17`
- **Class:** CWE-250 · OWASP A05  ·  **Detector:** `SEC-K8S-PRIVILEGED` (builtin, confidence high, verdict unverified)
- **Evidence:** `privileged: true`
- **Fix:** Drop privileged; use least-privilege securityContext + capability drops.

### [High] Command/code execution sink
- **Location:** `index.php:6`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-PHP-EXEC` (builtin, confidence high, verdict unverified)
- **Evidence:** `return shell_exec("tar -czf /tmp/" . $label . ".tgz /var/log/app");`
- **Fix:** Avoid these on untrusted input; use safe APIs and validation.

### [High] Insecure deserialization (unserialize)
- **Location:** `index.php:12`
- **Class:** CWE-502 · OWASP A08  ·  **Detector:** `SEC-PHP-UNSER` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return unserialize($raw);`
- **Fix:** Never unserialize untrusted data; use JSON. If the call has to stay, pass `['allowed_classes' => false]` so no object can be constructed.

### [High] SQL injection (superglobal in query)
- **Location:** `index.php:17`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-PHP-SQLI` (builtin, confidence high, verdict unverified)
- **Evidence:** `return mysqli_query($conn, "SELECT * FROM users WHERE id = " . $_GET['id']);`
- **Fix:** Use prepared statements (mysqli/PDO bind parameters).

### [High] `mem::transmute` reinterprets bytes with no checks
- **Location:** `lib.rs:16`
- **Class:** CWE-704 · OWASP A04  ·  **Detector:** `SEC-RS-TRANSMUTE` (builtin, confidence medium, verdict unverified)
- **Evidence:** `unsafe { mem::transmute::<[u8; 4], f32>(raw) }`
- **Fix:** Prefer a checked conversion (`TryFrom`, `from_le_bytes`, `bytemuck`); transmute assumes a layout the compiler does not verify.

### [High] Database publicly accessible
- **Location:** `main.tf:30`
- **Class:** CWE-284 · OWASP A01  ·  **Detector:** `SEC-TF-PUBLICDB` (builtin, confidence medium, verdict unverified)
- **Evidence:** `publicly_accessible = true`
- **Fix:** Keep managed databases in private subnets; never expose them publicly.

### [High] Database publicly accessible
- **Location:** `main.tf:36`
- **Class:** CWE-284 · OWASP A01  ·  **Detector:** `SEC-TF-PUBLICDB` (builtin, confidence medium, verdict unverified)
- **Evidence:** `publicly_accessible = true`
- **Fix:** Keep managed databases in private subnets; never expose them publicly.

### [High] Flutter accepts any TLS certificate
- **Location:** `net.dart:8`
- **Class:** CWE-295 · OWASP A02  ·  **Detector:** `SEC-DART-BADCERT` (builtin, confidence high, verdict unverified)
- **Evidence:** `client.badCertificateCallback = (cert, host, port) => true;`
- **Fix:** Never return true for all certs; pin or validate properly.

### [High] XXE (XML external entity resolution enabled)
- **Location:** `py_app.py:17`
- **Class:** CWE-611 · OWASP A05  ·  **Detector:** `SEC-PY-XXE` (builtin, confidence high, verdict unverified)
- **Evidence:** `parser = etree.XMLParser(resolve_entities=True, no_network=False)  # UNSAFE`
- **Fix:** Disable entities/DTD/network (defusedxml or resolve_entities=False).

### [High] Disabled TLS certificate verification
- **Location:** `py_app.py:24`
- **Class:** CWE-295 · OWASP A02  ·  **Detector:** `SEC-PY-TLS` (builtin, confidence high, verdict unverified)
- **Evidence:** `return requests.get(url, verify=False, timeout=5)  # UNSAFE: verify=False`
- **Fix:** Keep certificate verification on; trust the proper CA bundle.

### [High] OS command injection (subprocess shell=True)
- **Location:** `py_app.py:30`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-PY-CMDI` (builtin, confidence high, verdict unverified)
- **Evidence:** `return subprocess.call('ping -c 1 ' + host, shell=True)  # UNSAFE: shell=True + concat`
- **Reachability:** `L29: run_ping(host) (parameter) → L30: TAINT-PY-CMDI-SHELL argument 0`
- **Fix:** Use an argument list without shell=True and validate input.

### [High] SQL built by string concatenation or interpolation
- **Location:** `py_app.py:48`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-PY-SQLI-CONCAT` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return cursor.execute("SELECT * FROM users WHERE name = '" + name + "'")  # UNSAFE`
- **Fix:** Pass the value as a bind parameter — `cursor.execute(sql, (value,))`, or the ORM's own parameter form. Every string-building route into a query is the same bug: an f-string, `%`, `.format()` and `+` differ only in spelling.

### [High] Weak password hashing (MD5)
- **Location:** `server.js:29`
- **Class:** CWE-327 · OWASP A02  ·  **Detector:** `SEC-JS-MD5` (builtin, confidence high, verdict unverified)
- **Evidence:** `return crypto.createHash('md5').update(pw).digest('hex');`
- **Fix:** Use a memory-hard KDF (argon2id / bcrypt / scrypt) with a per-password salt.

### [High] Hardcoded AWS access key id
- **Location:** `server.js:33`
- **Class:** CWE-798 · OWASP A07  ·  **Detector:** `SEC-SECRET-AWS` (builtin, confidence high, verdict unverified)
- **Evidence:** `[redacted] possible secret detected here (value not shown)`
- **Fix:** Remove the secret from source, rotate it, and load from a secret manager / env.

### [High] Possible SSRF (server fetch of user-supplied URL)
- **Location:** `server.js:45`
- **Class:** CWE-918 · OWASP A10  ·  **Detector:** `SEC-JS-SSRF` (builtin, confidence high, verdict unverified)
- **Evidence:** `require('http').get(req.query.url, (r) => r.pipe(res));`
- **Reachability:** `L45: req.query.url (request) → L45: TAINT-JS-SSRF argument 0`
- **Fix:** Allowlist the destination host and block private / link-local ranges.

### [High] Prototype pollution — attacker-named key written to an object
- **Location:** `util.js:11`
- **Class:** CWE-1321 · OWASP A08  ·  **Detector:** `PROTO-JS-WRITE` (structural, confidence medium, verdict unverified)
- **Evidence:** `target[key] = merge(target[key] || {}, source[key]);   // UNSAFE: no key filtering`
- **Fix:** `merge()` writes to `target` under a key it did not choose — the index is `key`, which this function binds as an object key — and nothing in the function refuses `__proto__`, `constructor` or `prototype`. A caller who controls that key controls every object in the process. Skip those three names before the write, or build the result on `Object.create(null)` or a `Map`, where they are ordinary strings.

### [High] Prototype pollution — attacker-named key written to an object
- **Location:** `util.js:13`
- **Class:** CWE-1321 · OWASP A08  ·  **Detector:** `PROTO-JS-WRITE` (structural, confidence medium, verdict unverified)
- **Evidence:** `target[key] = source[key];`
- **Fix:** `merge()` writes to `target` under a key it did not choose — the index is `key`, which this function binds as an object key — and nothing in the function refuses `__proto__`, `constructor` or `prototype`. A caller who controls that key controls every object in the process. Skip those three names before the write, or build the result on `Object.create(null)` or a `Map`, where they are ordinary strings.

### [High] Server-side template injection (dynamic Function)
- **Location:** `util.js:28`
- **Class:** CWE-94 · OWASP A03  ·  **Detector:** `SEC-JS-SSTI` (builtin, confidence high, verdict unverified)
- **Evidence:** `` const fn = new Function('data', 'return `' + templateSource + '`');  // UNSAFE ``
- **Reachability:** `L27: templateSource (parameter) → L28: TAINT-JS-SSTI argument 1`
- **Fix:** Pass user data as template context; never compile it as code.

### [High] Insecure deserialization (Marshal.load)
- **Location:** `worker.rb:6`
- **Class:** CWE-502 · OWASP A08  ·  **Detector:** `SEC-RB-MARSHAL` (builtin, confidence high, verdict unverified)
- **Evidence:** `Marshal.load(blob)`
- **Fix:** Never Marshal.load untrusted data; use JSON.

### [High] Dynamic code execution (eval)
- **Location:** `worker.rb:11`
- **Class:** CWE-95 · OWASP A03  ·  **Detector:** `SEC-RB-EVAL` (builtin, confidence high, verdict unverified)
- **Evidence:** `eval(rule)`
- **Fix:** Never eval untrusted input.

### [Medium] Unpinned base image (:latest)
- **Location:** `Dockerfile:3`
- **Class:** CWE-1104 · OWASP A06  ·  **Detector:** `SEC-DOCKER-LATEST` (builtin, confidence high, verdict unverified)
- **Evidence:** `FROM node:latest`
- **Fix:** Pin the base image by version and ideally by @sha256 digest.

### [Medium] WebView JavaScript enabled
- **Location:** `Mobile.kt:14`
- **Class:** CWE-749 · OWASP A05  ·  **Detector:** `SEC-ANDROID-WEBVIEW-JS` (builtin, confidence medium, verdict unverified)
- **Evidence:** `web.settings.setJavaScriptEnabled(true)`
- **Fix:** Enable JS only when required and only for trusted content.

### [Medium] Possible SQL injection (concatenated SqlCommand)
- **Location:** `Repository.cs:21`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-CS-SQL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return new SqlCommand("SELECT * FROM Users WHERE Email = '" + email + "'");`
- **Fix:** Use parameterized queries (SqlParameter).

### [Medium] Possible SQL injection (concatenated query)
- **Location:** `Service.java:24`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-JAVA-SQL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return st.executeQuery("SELECT * FROM users WHERE email = '" + email + "'");`
- **Fix:** Use PreparedStatement with bind parameters.

### [Medium] Agent code-execution tool (Python REPL) exposed
- **Location:** `agent.py:3`
- **Class:** CWE-94 · OWASP A05  ·  **Detector:** `SEC-AI-PYREPL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `from langchain_experimental.tools import PythonREPLTool`
- **Fix:** Arbitrary-code tools give an LLM RCE; remove, or run in an isolated sandbox with no secrets.

### [Medium] Agent code-execution tool (Python REPL) exposed
- **Location:** `agent.py:16`
- **Class:** CWE-94 · OWASP A05  ·  **Detector:** `SEC-AI-PYREPL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return [PythonREPLTool()]`
- **Fix:** Arbitrary-code tools give an LLM RCE; remove, or run in an isolated sandbox with no secrets.

### [Medium] Agent shell/terminal tool exposed to the model
- **Location:** `agent.py:22`
- **Class:** CWE-78 · OWASP A05  ·  **Detector:** `SEC-AI-SHELL-TOOL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `return load_tools(["terminal"], llm=llm)`
- **Fix:** Giving an LLM a shell is excessive agency; drop it or constrain to an allowlisted command set.

### [Medium] Possible SQL injection (Sprintf into SQL)
- **Location:** `api.go:22`
- **Class:** CWE-89 · OWASP A03  ·  **Detector:** `SEC-GO-SQL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `q := fmt.Sprintf("SELECT id, email FROM users WHERE name = '%s'", name)`
- **Fix:** Use parameterized queries (database/sql placeholders).

### [Medium] OS command execution (exec.Command)
- **Location:** `api.go:29`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-GO-EXEC` (builtin, confidence medium, verdict unverified)
- **Evidence:** `out, _ := exec.Command("ping", "-c", "1", host).Output()`
- **Fix:** Validate arguments; never pass user input to a shell.

### [Medium] Open redirect (user-controlled Location)
- **Location:** `auth.js:20`
- **Class:** CWE-601 · OWASP A01  ·  **Detector:** `SEC-JS-OPENREDIR` (builtin, confidence high, verdict unverified)
- **Evidence:** `res.writeHead(302, { Location: req.query.next });   // ?next=https://evil.example`
- **Reachability:** `L20: req.query.next (request) → L20: TAINT-JS-OPENREDIR argument 1`
- **Fix:** Redirect only to an allowlist of relative paths.

### [Medium] Host network / PID namespace shared
- **Location:** `deploy.yaml:11`
- **Class:** CWE-668 · OWASP A05  ·  **Detector:** `SEC-K8S-HOSTNET` (builtin, confidence medium, verdict unverified)
- **Evidence:** `hostNetwork: true`
- **Fix:** Do not share the host network/PID/IPC namespaces.

### [Medium] Privilege escalation allowed
- **Location:** `deploy.yaml:20`
- **Class:** CWE-250 · OWASP A05  ·  **Detector:** `SEC-K8S-PRIVESC` (builtin, confidence medium, verdict unverified)
- **Evidence:** `allowPrivilegeEscalation: true`
- **Fix:** Set allowPrivilegeEscalation: false.

### [Medium] hostPath volume mount
- **Location:** `deploy.yaml:27`
- **Class:** CWE-552 · OWASP A05  ·  **Detector:** `SEC-K8S-HOSTPATH` (builtin, confidence medium, verdict unverified)
- **Evidence:** `hostPath:`
- **Fix:** Avoid hostPath; use PVCs / restricted volumes.

### [Medium] `unsafe` block without a safety comment
- **Location:** `lib.rs:8`
- **Class:** CWE-758 · OWASP A04  ·  **Detector:** `SEC-RS-UNSAFE` (builtin, confidence medium, verdict unverified)
- **Evidence:** `unsafe {`
- **Fix:** Document the invariants that make this block sound in a `// SAFETY:` comment directly above it, or replace it with a safe abstraction.

### [Medium] `unsafe` block without a safety comment
- **Location:** `lib.rs:16`
- **Class:** CWE-758 · OWASP A04  ·  **Detector:** `SEC-RS-UNSAFE` (builtin, confidence medium, verdict unverified)
- **Evidence:** `unsafe { mem::transmute::<[u8; 4], f32>(raw) }`
- **Fix:** Document the invariants that make this block sound in a `// SAFETY:` comment directly above it, or replace it with a safe abstraction.

### [Medium] Shell command built with a shell interpreter
- **Location:** `lib.rs:22`
- **Class:** CWE-78 · OWASP A03  ·  **Detector:** `SEC-RS-CMDI` (builtin, confidence medium, verdict unverified)
- **Evidence:** `let _ = Command::new("sh").arg("-c").arg(format!("report --for {}", label)).status();`
- **Fix:** Invoke the program directly with `Command::new(prog).arg(value)`; a `-c` string re-introduces shell metacharacter injection that Rust otherwise avoids.

### [Medium] Security group open to the world
- **Location:** `main.tf:9`
- **Class:** CWE-284 · OWASP A01  ·  **Detector:** `SEC-TF-OPENINGRESS` (builtin, confidence medium, verdict unverified)
- **Evidence:** `cidr_blocks = ["0.0.0.0/0"]`
- **Fix:** Restrict ingress to known CIDRs; avoid 0.0.0.0/0 on sensitive ports.

### [Medium] Public-read storage ACL
- **Location:** `main.tf:15`
- **Class:** CWE-732 · OWASP A01  ·  **Detector:** `SEC-TF-PUBLICACL` (builtin, confidence medium, verdict unverified)
- **Evidence:** `acl    = "public-read"`
- **Fix:** Keep buckets private; use signed URLs / explicit policies.

### [Medium] Over-broad IAM policy (Action/Resource "*")
- **Location:** `main.tf:22`
- **Class:** CWE-732 · OWASP A01  ·  **Detector:** `SEC-TF-IAM-WILDCARD` (builtin, confidence medium, verdict unverified)
- **Evidence:** `Statement = [{ Effect = "Allow", "Action" = "*", "Resource" = "*" }]`
- **Fix:** Scope IAM actions and resources to the minimum needed.

### [Medium] Storage encryption disabled
- **Location:** `main.tf:29`
- **Class:** CWE-311 · OWASP A02  ·  **Detector:** `SEC-TF-UNENCRYPTED` (builtin, confidence medium, verdict unverified)
- **Evidence:** `storage_encrypted = false`
- **Fix:** Enable encryption at rest.

### [Medium] SSRF — server fetches an untrusted URL
- **Location:** `py_app.py:24`
- **Class:** CWE-918 · OWASP A10  ·  **Detector:** `TAINT-PY-SSRF` (taint, confidence medium, verdict unverified)
- **Evidence:** `return requests.get(url, verify=False, timeout=5)  # UNSAFE: verify=False`
- **Reachability:** `L23: fetch_secure(url) (parameter) → L24: TAINT-PY-SSRF argument 0`
- **Fix:** Allowlist the destination host; block private/link-local ranges.

### [Medium] Possible hardcoded credential
- **Location:** `server.js:34`
- **Class:** CWE-798 · OWASP A07  ·  **Detector:** `SEC-SECRET-GENERIC` (builtin, confidence medium, verdict unverified)
- **Evidence:** `[redacted] possible secret detected here (value not shown)`
- **Fix:** If real, rotate it and load from env / a secret manager (ignore obvious placeholders).

### [Medium] Permissive CORS reflecting Origin
- **Location:** `server.js:38`
- **Class:** CWE-942 · OWASP A05  ·  **Detector:** `SEC-JS-CORS` (builtin, confidence high, verdict unverified)
- **Evidence:** `res.header('Access-Control-Allow-Origin', req.headers.origin);`
- **Fix:** Reflect only an explicit origin allowlist; never echo the request Origin with credentials.

## Notes

- Tier-0 (deterministic, no LLM). Business-logic flaws — the rules being broken are the product's, and they are not written down anywhere the analyzer can read — remain outside this tier; run with an LLM backend for triage + logic-bug discovery.
- Taint analysis follows a value across calls to functions resolved by simple name, and across import edges into other files in the scanned set, to any depth — but only files that were scanned. A chain that passes through an excluded directory, a third-party package, or a language without taint depth stops there. In JavaScript only named declarations and named function/arrow expressions carry a summary — not object-property or class methods — and a namespace import (`const u = require('./util'); u.run(x)`) is not resolved.
- Function parameters are treated as a weak (MEDIUM-confidence) source because whether they carry untrusted data depends on callers, which are not analyzed. Two shapes are excepted and rated HIGH, because the framework is the caller and it binds them from the request: a handler under a routing decorator (`@app.get`, `@router.post`, `@bp.route`), and a Django view, recognised by a first parameter named `request`. A route registered some other way — a `urls.py` entry pointing at an undecorated function whose first parameter is not `request`, a class-based view's `get`/`post` — is not recognised, and its parameters stay a MEDIUM lead.
- SQL reached through an ORM is only seen at the escape hatches the ORM provides: `.raw()`, `.extra()`, `execute()`/`executemany()`/`exec_driver_sql()` on a receiver named like a connection or session (cursor, conn, connection, session, db, database, engine, pool, tx, trans). A database reached through a receiver this list does not name, or through a call whose receiver is not a plain dotted name (`get_conn().execute(sql)`), is not matched.
- Outbound HTTP is read from the written call: the `requests`, `httpx` and `urllib` functions by dotted name, and `request`/`putrequest` on a receiver named like a connection or a client (conn, connection, http, https, urllib3, pool, manager, client, httpx, requests). `.get()` and `.post()` on a receiver are deliberately NOT sinks and `session` is deliberately not a receiver: they are the most common method and variable names in Python, and `session.get(key)` is a Flask dictionary lookup rather than a request leaving the machine. A client reached only that way is a documented miss.
- A validation guard (`if (bad(x)) return/throw`) is assumed to sanitize; an ineffective guard therefore hides its sink from this tier.
- The JavaScript/TypeScript scanner is a brace-aware statement scanner, not a parser. Flat destructuring of a tainted value (`const { name } = req.query`, including renames, defaults and rest) is followed; a NESTED pattern (`const { a: { b } } = req.body`) is not, and neither are cross-boundary closures, dynamic property access or JSX.
- In JavaScript a value stored on `this` is not followed: a constructor that keeps a parameter as `this.file` and a method that later runs `exec('convert ' + this.file)` are two scopes with nothing carried between them. Object fields are the largest remaining shape in the command-injection class on SecBench.js and are named here rather than counted as covered.
- Which function `exec` is depends on what the file imported, so the shell sinks are resolved per file: a receiver bound to `child_process` or `shelljs` — including a promisified alias and the `child_process_1` form TypeScript emits — is a shell, and `pattern.exec(text)` in a file that imported neither is a regular expression. A receiver assigned indirectly (`const cp = deps.child_process`) is not resolved.
- Structural analysis (JavaScript, Python, TypeScript only) decides 11 questions about a request handler that no single line carries: whether a handler that knows its caller ignores them when selecting a row, whether a state-changing handler establishes a caller at all, whether a credential-testing endpoint bounds attempts, whether an upload is checked before it is written, whether the caller chooses which fields get written, whether an export hands a spreadsheet a cell it will run, whether a failure message says which half of the credential was wrong, whether a branch is decided by something the caller sent, and whether a sensitive value is stored as it arrived, whether the response describes the server, and whether the caller chooses how much work to do. Evidence is followed through functions defined in the same module; a helper imported from elsewhere is not followed, and the handler is left unreported rather than assumed unprotected.
- Ownership is judged by whether the principal constrains the query at all, never by whether it constrains it correctly — a handler that filters on the wrong identity is outside what this decides. Any call that receives the principal is treated as a check delegated, which is deliberately generous: the stricter reading was measured against the corpus and recovered no true positives while adding false ones.
- Rate-limit analysis reports only credential-testing endpoints (login, registration, password reset, token and OTP issuance) that reach a credential check with no limiter in the handler, its decorators, its module-local helpers, or registered on the app. A missing limit on any other endpoint is a capacity decision this does not make, and a limiter enforced outside the application — at a gateway, a WAF or a reverse proxy — is invisible here and will read as missing.
- A login the project does not write is read separately, and only in one shape: Django's own `LoginView` or `PasswordResetView` mounted in a URL conf, in a project where nothing at all names a limiter. Two neighbouring shapes are NOT reported and are genuine misses rather than decisions — `include('django.contrib.auth.urls')`, which mounts the same login through a module this analysis does not open, and a class-based view the project wrote itself, whose body no rule here reads.
- Upload analysis reports a handler that reads an uploaded file and writes it with no validation between the two. It judges whether a check is present, never whether the check is adequate: an allowlist containing a dangerous type, or an extension test on a name the deployment later re-derives, both read as validated.
- Mass-assignment analysis reports a request-supplied mapping spread into a persisted object with no field allowlist. A schema, serializer or typed body counts as the allowlist, so a handler that binds the body to a declared field set is never reported — including one whose schema declares a field it should not accept.
- CSV export analysis reports one export per file: a function producing CSV whose rows carry non-literal values, where nothing in the module neutralises a leading =, +, - or @. It does not decide whether those values are reachable by a user — a CSV of machine-generated identifiers reads the same as one of free-text titles — and it goes silent for the whole file when any neutralising helper is present, including one that the offending export does not call.
- Account-enumeration analysis reads the message vocabulary a handler can return and reports one that names the identity in one branch and the credential in another, or one that discloses account existence inside a named credential flow. It decides nothing about timing: a handler with a single message that only hashes a password when the account exists is still enumerable, and is not reported here.
- Client-trust analysis reports a header or cookie compared against a literal where the comparison decides a branch. Transport headers are excluded by name (content type, accept, user agent, X-Requested-With, referer, origin, host), and the whole file is left alone when anything in it verifies a signature — so a module that authenticates one header and trusts another is not reported for the second.
- Cleartext-storage analysis reports a write to durable storage whose payload names a sensitive field, in a module where nothing encrypts, hashes, redacts or tokenises. It covers files and object stores only: a sensitive value in a response is CWE-200 and one in a log is CWE-532, both reported elsewhere or not at all. It does not decide whether the storage is reachable, and it goes silent for a whole module that protects any value anywhere in it.
- Response-exposure analysis reports a caught exception or an environment/settings/path value reaching a response body the handler builds itself. A value passed to a template is not reported — the template decides what it renders — and neither is a framework debug page, which is a deployment setting rather than a line of code. It cannot tell a harmless exception message from a revealing one, because which exceptions arrive is a runtime fact.
- Resource analysis reports a request-supplied number reaching an allocation — a range, a repeat, a buffer, a sleep — where nothing in the function bounds it. It cannot price the work: a loop building ten small dicts reads the same as one allocating a megabyte each. Any comparison of the value against a numeric literal counts as a bound, including one that rejects rather than clamps.
- JavaScript/TypeScript structural analysis recognises a route by a mount carrying a string literal path (`app.post('/x', ...)`), a NestJS method decorator, or a Next.js route/Pages-API export. A router assembled dynamically, a path held in a constant, or a framework not in that list is not recognised, and its handlers are not analysed at all.
- It has no parser. The whole mount call is read as the handler, so an inline handler and its own middleware are not told apart — which can only silence a rule, never fire one — and evidence is followed only into named functions defined in the same file.
- Unlike the Python rules, none of these is measured against an external corpus: RealVuln v1 is Python-only. What is asserted is that the shapes in the test suite are found and that the shipped secure fixture stays silent.
- The response-exposure, client-trusted-decision and account-enumeration rules were added with that bound measured rather than assumed: across four corpora they moved no figure at all. They fire — 15 findings on CVEfixes, none of which landed on a labelled hunk — and they are silent across 1,372,511 lines of maintained code, which holds no Express application for them to read. Treat them as unmeasured in the strongest sense: nothing outside this repository's own fixtures has ever agreed or disagreed with them.
- Prototype pollution is decided inside one named function: a write whose key the function bound by iterating an object, by an iteration callback's parameter, by splitting a path, or by walking one level at a time into the object it then writes to — with no `__proto__` / `constructor` / `prototype` refusal, no null-prototype target and no `Map` anywhere in that function. A merge split across modules and a guard that lives in an imported helper are both outside it; the second silences the rule rather than firing it.
- ReDoS analysis (JavaScript, Python, TypeScript only) decides backtracking cost from the regex's structure — star height above one and repeated groups with overlapping alternatives for the exponential case, and two unbounded repeats over overlapping characters followed by something that can fail for the quadratic one. In Python it reads patterns written at the call site or bound to a module-level constant; in JavaScript and TypeScript it reads regex literals and the string argument to `new RegExp`. A pattern built at runtime or loaded from configuration is not analysed in either.
- A quadratic finding is a claim about the regular expression and not about the subject it runs on. It costs quadratic time on input that fails to match, so it is a denial of service where that input is attacker-supplied and unbounded and a performance note where it is neither. Nothing in this analysis knows which one the caller has.
- A regex this analysis does not report is not certified safe: the criteria are chosen to under-report rather than to be complete, and an exact answer needs the automaton.

> A report with no findings means these rules did not fire and no taint path was proven, across the files that were scanned. It does not mean the code is safe.
