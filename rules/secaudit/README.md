# SecAudit rules for Semgrep

<!-- Generated by `python3 scripts/gen_semgrep_pack.py`. Do not edit by hand. -->

61 of SecAudit's 115 detectors, as Semgrep rules.

```bash
semgrep --config rules/secaudit/
```

Or from a checkout of this repository, pinned:

```yaml
# .semgrep.yml
rules:
  - ...   # or: semgrep --config https://raw.githubusercontent.com/mtvrkan/secaudit/v1.0.0/rules/secaudit/
```

## What is here

| Group | Rules | Applies to |
|---|---|---|
| `ai` | 1 | *.py |
| `ci` | 1 | *.yaml, *.yml |
| `compose` | 1 | *.yaml, *.yml |
| `cors` | 1 | *.cjs, *.cts, *.go, *.java, *.js, *.jsx, *.mjs, *.mts, *.php, *.phtml, *.py, *.rb, *.ts, *.tsx |
| `dart` | 1 | *.dart |
| `docker` | 4 | Dockerfile |
| `go` | 1 | *.go |
| `header` | 1 | *.cjs, *.cts, *.js, *.jsx, *.mjs, *.mts, *.py, *.ts, *.tsx |
| `ios` | 1 | *.plist, *.swift, *.xml |
| `js` | 7 | *.cjs, *.cts, *.js, *.jsx, *.mjs, *.mts, *.ts, *.tsx |
| `php` | 7 | *.php, *.phtml |
| `py` | 11 | *.py |
| `rs` | 1 | *.rs |
| `secret` | 13 | *.cjs, *.cs, *.cts, *.env, *.go, *.java, *.js, *.json, *.jsx, *.mjs, *.mts, *.php, *.phtml, *.py, *.rb, *.tf, *.ts, *.tsx, *.txt, *.yaml, *.yml, Dockerfile |
| `supply` | 1 | *.sh, *.yaml, *.yml |
| `tf` | 5 | *.json, *.tf, *.yaml, *.yml |
| `tpl` | 4 | *.htm, *.html, *.j2, *.jinja, *.jinja2 |

## What is deliberately not here

54 detectors are withheld because Semgrep's `pattern-regex` cannot reproduce them faithfully, and a rule that is noisier than the one it claims to be is worse than an absent rule — it would make the precision numbers this project publishes describe something nobody is running.

| Detector | Why it is not exported |
|---|---|
| `SEC-JS-SSRF` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-XSS` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-PATHTRAV` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-MASSASSIGN` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-EVAL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-SSTI` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-XXE` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-TLS` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-CMDI` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-PICKLE` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-RANDOM` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-YAML` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-OSSYSTEM` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-EVAL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-MD5` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-DEBUG` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-DEBUG-CONFIG` | dropped when `^\s*#` appears on the MATCHED LINE; `pattern-not-regex` filters the matched range and the match here is a few characters wide, so the exported rule would keep every finding this one drops — measured on one checkout, that is 80 findings of 220 |
| `SEC-PY-DEBUG-DEFAULT` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-COOKIE-FLAGS` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-COOKIE-NO-SECURE` | cleared when `set_cookie\s*\((?:[^()]|\([^()]*\))*secure\s*=\s*True` appears anywhere in the file; `pattern-not-regex` filters the matched text, not the file |
| `SEC-PY-COOKIE-SETTINGS-NO-SECURE` | cleared when `(?:SESSION|CSRF)_COOKIE_SECURE\s*=\s*True` appears anywhere in the file; `pattern-not-regex` filters the matched text, not the file |
| `SEC-PY-WEAK-PRNG` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-CSRF-EXEMPT` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-AUTOESCAPE-OFF` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-JINJA-ENV-DEFAULT` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-SQL-TRACE` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PY-MD5-IMPORTED` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-GO-TLS` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-GO-EXEC` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-RS-UNSAFE` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-RS-TRANSMUTE` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JAVA-DESER` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JAVA-EXEC` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JAVA-SQL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PHP-EXEC` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-PHP-SQLI` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-RB-MARSHAL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-RB-EVAL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-CS-DESER` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-CS-SQL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-K8S-PRIVILEGED` | raised only when `^\s*(?:apiVersion|kind|services)\s*:` appears anywhere in the file, which is how the rule knows it is looking at a manifest rather than at any other document shipped as YAML; Semgrep's `patterns:` conditions all have to match the same range, and `paths:` filters by filename, so the exported rule would carry the pattern without the precondition and fire on both |
| `SEC-K8S-PRIVESC` | raised only when `^\s*(?:apiVersion|kind|services)\s*:` appears anywhere in the file, which is how the rule knows it is looking at a manifest rather than at any other document shipped as YAML; Semgrep's `patterns:` conditions all have to match the same range, and `paths:` filters by filename, so the exported rule would carry the pattern without the precondition and fire on both |
| `SEC-K8S-HOSTPATH` | raised only when `^\s*(?:apiVersion|kind|services)\s*:` appears anywhere in the file, which is how the rule knows it is looking at a manifest rather than at any other document shipped as YAML; Semgrep's `patterns:` conditions all have to match the same range, and `paths:` filters by filename, so the exported rule would carry the pattern without the precondition and fire on both |
| `SEC-K8S-HOSTNET` | raised only when `^\s*(?:apiVersion|kind|services)\s*:` appears anywhere in the file, which is how the rule knows it is looking at a manifest rather than at any other document shipped as YAML; Semgrep's `patterns:` conditions all have to match the same range, and `paths:` filters by filename, so the exported rule would carry the pattern without the precondition and fire on both |
| `SEC-COOKIE-INSECURE` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-ANDROID-JSIF` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-ANDROID-WORLDPERM` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-ANDROID-WEBVIEW-JS` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-AI-LANGCHAIN-DANGER` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-AI-PYREPL` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-AI-LLM-EXEC` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-TPL-FORM-NO-CSRF` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-DOMXSS` | matches a view with comments and string literals blanked; `pattern-regex` runs on raw text, so the exported rule would fire inside a comment or a string and be noisier than the original |
| `SEC-JS-HTML-CONCAT` | dropped when `\b(?:escapeHtml|escapeHTML|htmlEscape|htmlEncode|encodeHTML|escapeExpression|sanitizeHtml|DOMPurify|htmlspecialchars)\s*\(|\b(?:Messages|Strings|i18n|I18n)\s*[.\[]|\b(?:gettext|ngettext|__|_e|trans|translate)\s*\(` appears on the MATCHED LINE; `pattern-not-regex` filters the matched range and the match here is a few characters wide, so the exported rule would keep every finding this one drops — measured on one checkout, that is 80 findings of 220 |

Running SecAudit itself is how you get these. That is a real difference in what the two tools can express, not a packaging decision:

```bash
pip install secaudit-kit && secaudit .
```

## Fidelity

Every exported rule carries the same regex as the detector it came from, with the Python flags made inline (`(?im)`, or `(?m)` where the detector is case-sensitive because the token shape encodes a fixed case). [`kit/tests/test_semgrep_pack.py`](../../kit/tests/test_semgrep_pack.py) applies each exported pattern to the shipped fixtures and requires the exact same set of `(file, line)` hits as the detector — so this is checked equivalence, not a claim that the translation looked right.

Two differences remain, and neither is fixable from this side:

- **Severity** — SecAudit has five levels, Semgrep has three. Critical and High both become `ERROR`; the original is preserved in `metadata.secaudit-severity`.
- **Secret redaction** — SecAudit never prints a matched credential. Semgrep prints the matching line. Rules that match credential material are marked in the YAML.

## No taint analysis here

Source→sink dataflow is not part of this pack. These are the pattern rules only. A finding from the taint tier states where untrusted input entered and every hop to the dangerous call, across function and module boundaries — see [language coverage](../../docs/language-coverage.md). Semgrep has its own taint mode; this pack does not attempt to translate into it, because a half-translated dataflow rule proves nothing and reads as though it does.

