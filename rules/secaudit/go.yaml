# SecAudit detector pack — `go` group (1 rules).
#
# Generated by `python3 scripts/gen_semgrep_pack.py` from kit/secaudit_core/detectors.py.
# Do not edit by hand; CI fails on drift. See ../README.md for what is deliberately
# NOT in this pack and why.
rules:
  - id: secaudit.sec-go-sql
    message: 'Possible SQL injection (Sprintf into SQL) (CWE-89, OWASP A03). Use parameterized queries (database/sql placeholders).'
    languages: [regex]
    severity: WARNING
    patterns:
      - pattern-regex: '(?im)fmt\.Sprintf\([^)]*\b(?:SELECT|INSERT|UPDATE|DELETE)\b'
    paths:
      include:
        - '*.go'
    metadata:
      cwe: 'CWE-89'
      owasp: 'A03'
      secaudit-id: 'SEC-GO-SQL'
      secaudit-severity: 'Medium'
      confidence: 'MEDIUM'
      category: security
      source: 'https://github.com/mtvrkan/secaudit'
